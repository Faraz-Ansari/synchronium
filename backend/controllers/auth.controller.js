export const signup = async (req, res) => {
    res.json({ message: "Hello from the signup controller" });
};

export const login = async (req, res) => {
    res.json({ message: "Hello from the login controller" });
};

export const logout = async (req, res) => {
    res.json({ message: "Hello from the logout controller" });
};
